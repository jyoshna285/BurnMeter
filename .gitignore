# Ignore Python cache
__pycache__/
*.pyc

# Ignore development environment secrets
.env

# Ignore logs
*.log

# Allow users.db (your SQLite file)
# Previously ignored: users.db and *.sqlite3 — we remove those
# So that Railway/GitHub can include it in deployment

# Optional: ignore runtime-specific folders
instance/
.DS_Store
